export interface Space{
    URI: string;
    name: string;
}

export interface GFR_Element{
    spaceURI: string;
    spaceName: string;
    floorArea: string;
    windowArea: string;
    pct: number;
}